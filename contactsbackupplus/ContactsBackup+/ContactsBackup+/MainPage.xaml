<phone:PhoneApplicationPage
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:edc="clr-namespace:Microsoft.Expression.Controls;assembly=Microsoft.Expression.Drawing"
    xmlns:maps="clr-namespace:Microsoft.Phone.Maps.Controls;assembly=Microsoft.Phone.Maps"
    x:Class="ContactsBackup_.MainPage"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="True" Background="#FF574141">
    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar>
            <shell:ApplicationBarIconButton IconUri="/Assets/AppBar/sync.png" IsEnabled="True" Text="refresh"/>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="#FFE5E4E5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" MinHeight="228"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!-- LOCALIZATION NOTE:
            To localize the displayed strings copy their values to appropriately named
            keys in the app's neutral language resource file (AppResources.resx) then
            replace the hard-coded text value between the attributes' quotation marks
            with the binding clause whose path points to that string name.

            For example:

                Text="{Binding Path=LocalizedResources.ApplicationTitle, Source={StaticResource LocalizedStrings}}"

            This binding points to the template's string resource named "ApplicationTitle".

            Adding supported languages in the Project Properties tab will create a
            new resx file per language that can carry the translated values of your
            UI strings. The binding in these examples will cause the value of the
            attributes to be drawn from the .resx file that matches the
            CurrentUICulture of the app at run time.
         -->

        <!--Uncomment to see an alignment grid to help ensure your controls are
            aligned on common boundaries.  The image has a top margin of -32px to
            account for the System Tray. Set this to 0 (or remove the margin altogether)
            if the System Tray is hidden.

            Before shipping remove this XAML and the image itself.-->
        <!--<Image Source="/Assets/AlignmentGrid.png" VerticalAlignment="Top" Height="800" Width="480" Margin="0,-32,0,0" Grid.Row="0" Grid.RowSpan="2" IsHitTestVisible="False" />-->
        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel x:Name="application" Grid.Row="0" Margin="8,17,4,28" Background="#FFAEAEAE">
            <TextBlock Text="Contacts Backup +" Style="{StaticResource PhoneTextNormalStyle}" Margin="12,0"/>
            <TextBlock Text="Profile" Margin="9,-7,0,0" Style="{StaticResource PhoneTextTitle1Style}"/>
            <Path Data="M249,105" Fill="#FFF4F4F5" HorizontalAlignment="Left" Height="0" Margin="249,0,0,0" Stretch="Fill" Stroke="Black" UseLayoutRounding="False" Width="0"/>
        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Grid.Row="1" Background="#FFE5E4E5" Height="515" VerticalAlignment="Bottom">
            <Grid HorizontalAlignment="Left" Height="152" VerticalAlignment="Top" Width="480"/>
            <Grid HorizontalAlignment="Left" Height="152" VerticalAlignment="Top" Width="480" Margin="0,152,0,0"/>
            <Grid HorizontalAlignment="Left" Height="152" VerticalAlignment="Top" Width="480" Margin="0,-208,0,0">
                <Grid.Background>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="Black" Offset="0"/>
                        <GradientStop Color="#FFC11A1A" Offset="1"/>
                    </LinearGradientBrush>
                </Grid.Background>
                <Button Content="Backup Contacts" Foreground="AliceBlue"  x:Name="Backup_drew" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="3,287,0,-251" Width="476" Click="Backup_Contacts" FontWeight="Bold" FontSize="26.667" Background="Black" Height="116" Opacity="0.335"/>
                <Button Content="Restore Contacts" Foreground="AliceBlue" x:Name="Restore_drew" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="3,431,0,-395" Width="476" Click="Restore_Contacts" FontWeight="Bold" FontSize="26.667" Background="Black" Height="116" Opacity="0.335"/>
                <Button Content="View Contacts Online" Foreground="AliceBlue" x:Name="View_drew" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="3,570,0,-534" Width="476" Click="View_Contacts_Online" FontWeight="Bold" FontSize="26.667" Background="Black" Height="116" Opacity="0.335"/>
            </Grid>
        </Grid>

    </Grid>

</phone:PhoneApplicationPage>